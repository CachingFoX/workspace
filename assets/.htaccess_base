RewriteEngine on

RewriteBase /

AuthUserFile /www/htdocs/w01810fb/trackables.wir-machen-wege.de/.htpasswd
AuthGroupFile /dev/null
AuthName 'bitte Zugangsdaten eingeben'
AuthType Basic
require valid-user

# Redirect /oc.html https://flopp.grus.uberspace.de/oc-monitor/
# Redirect /heatmap https://flopp.grus.uberspace.de/oc-heatmap/
# Redirect /safari https://safari.flopp.net

### force HTTPS
# RewriteCond %{HTTPS} !=on
# RewriteCond %{ENV:HTTPS} !=on
# RewriteRule .* https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]

# 10 minutes caching
<filesMatch "\.(html|json)$">
    Header set Cache-Control "no-cache, max-age=0, must-revalidate"
</filesMatch>
